"""Icon resolution for Yoto playlist tracks."""

from __future__ import annotations


def resolve_icons(playlist, api) -> dict:
    """Resolve icon IDs for each track in the playlist. Returns dict of filename -> icon_id."""
    return {}
